# tool-zone / mcp-ticketing — MCP streamable-HTTP server on 7005. Writes operational DB.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-ticketing
  namespace: tool-zone
  labels: {app.kubernetes.io/name: mcp-ticketing, zone: tool-zone, tier: tool}
spec:
  replicas: 1
  selector:
    matchLabels: {app.kubernetes.io/name: mcp-ticketing}
  template:
    metadata:
      labels: {app.kubernetes.io/name: mcp-ticketing, zone: tool-zone, tier: tool}
    spec:
      containers:
        - name: server
          image: soc-tool
          command: ["python", "services/mcp-ticketing/server.py"]
          ports:
            - {name: mcp-http, containerPort: 7005}
          env:
            - {name: MCP_LISTEN_HOST, value: "0.0.0.0"}
            - {name: MCP_LISTEN_PORT, value: "7005"}
          envFrom:
            - configMapRef: {name: data-endpoints}
          resources:
            requests: {cpu: 100m, memory: 128Mi}
            limits: {cpu: 500m, memory: 512Mi}
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-ticketing
  namespace: tool-zone
  labels: {app.kubernetes.io/name: mcp-ticketing, zone: tool-zone, tier: tool}
spec:
  selector: {app.kubernetes.io/name: mcp-ticketing}
  ports:
    - {name: mcp-http, port: 7005, targetPort: mcp-http}
