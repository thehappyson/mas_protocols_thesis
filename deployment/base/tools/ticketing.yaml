# tool-zone / mcp-ticketing — MCP tool server, streamable HTTP on 7005. Internals stubbed.
# Placeholder image: REPLACE_ME/mcp-ticketing:dev
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-ticketing
  namespace: tool-zone
  labels:
    app.kubernetes.io/name: mcp-ticketing
    zone: tool-zone
    tier: tool
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mcp-ticketing
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mcp-ticketing
        zone: tool-zone
        tier: tool
    spec:
      containers:
        - name: server
          image: REPLACE_ME/mcp-ticketing:dev
          ports:
            - name: mcp-http
              containerPort: 7005
          env:
            - name: MCP_TRANSPORT
              value: streamable-http
            - name: MCP_LISTEN_PORT
              value: "7005"
            - name: TTP_ENDPOINT
              value: https://trust-anchor.platform-zone.svc.cluster.local:8443
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://otel-collector.platform-zone.svc.cluster.local:4317
          envFrom:
            - configMapRef:
                name: data-endpoints
          # Unprofiled guesses — tune after first load tests.
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-ticketing
  namespace: tool-zone
  labels:
    app.kubernetes.io/name: mcp-ticketing
    zone: tool-zone
    tier: tool
spec:
  selector:
    app.kubernetes.io/name: mcp-ticketing
  ports:
    - name: mcp-http
      port: 7005
      targetPort: mcp-http
