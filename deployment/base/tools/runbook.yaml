# tool-zone / mcp-runbook — MCP streamable-HTTP server on 7004. Reads operational DB.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-runbook
  namespace: tool-zone
  labels: {app.kubernetes.io/name: mcp-runbook, zone: tool-zone, tier: tool}
spec:
  replicas: 1
  selector:
    matchLabels: {app.kubernetes.io/name: mcp-runbook}
  template:
    metadata:
      labels: {app.kubernetes.io/name: mcp-runbook, zone: tool-zone, tier: tool}
    spec:
      containers:
        - name: server
          image: soc-tool
          command: ["python", "services/mcp-runbook/server.py"]
          ports:
            - {name: mcp-http, containerPort: 7004}
          env:
            - {name: MCP_LISTEN_HOST, value: "0.0.0.0"}
            - {name: MCP_LISTEN_PORT, value: "7004"}
          envFrom:
            - configMapRef: {name: data-endpoints}
          resources:
            requests: {cpu: 100m, memory: 128Mi}
            limits: {cpu: 500m, memory: 512Mi}
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-runbook
  namespace: tool-zone
  labels: {app.kubernetes.io/name: mcp-runbook, zone: tool-zone, tier: tool}
spec:
  selector: {app.kubernetes.io/name: mcp-runbook}
  ports:
    - {name: mcp-http, port: 7004, targetPort: mcp-http}
