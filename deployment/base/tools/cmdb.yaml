# tool-zone / mcp-cmdb — MCP streamable-HTTP server on 7002. Reads operational DB.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-cmdb
  namespace: tool-zone
  labels: {app.kubernetes.io/name: mcp-cmdb, zone: tool-zone, tier: tool}
spec:
  replicas: 1
  selector:
    matchLabels: {app.kubernetes.io/name: mcp-cmdb}
  template:
    metadata:
      labels: {app.kubernetes.io/name: mcp-cmdb, zone: tool-zone, tier: tool}
    spec:
      containers:
        - name: server
          image: soc-tool
          command: ["python", "services/mcp-cmdb/server.py"]
          ports:
            - {name: mcp-http, containerPort: 7002}
          env:
            - {name: MCP_LISTEN_HOST, value: "0.0.0.0"}
            - {name: MCP_LISTEN_PORT, value: "7002"}
          envFrom:
            - configMapRef: {name: data-endpoints}
          resources:
            requests: {cpu: 100m, memory: 128Mi}
            limits: {cpu: 500m, memory: 512Mi}
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-cmdb
  namespace: tool-zone
  labels: {app.kubernetes.io/name: mcp-cmdb, zone: tool-zone, tier: tool}
spec:
  selector: {app.kubernetes.io/name: mcp-cmdb}
  ports:
    - {name: mcp-http, port: 7002, targetPort: mcp-http}
