# agent-zone / response-agent — A2A server on 9104 (consults verification).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: response-agent
  namespace: agent-zone
  labels: {app.kubernetes.io/name: response-agent, zone: agent-zone, tier: agent}
spec:
  replicas: 1
  selector:
    matchLabels: {app.kubernetes.io/name: response-agent}
  template:
    metadata:
      labels: {app.kubernetes.io/name: response-agent, zone: agent-zone, tier: agent}
    spec:
      containers:
        - name: agent
          image: soc-agent
          command: ["python", "services/response-agent/server.py"]
          ports:
            - {name: a2a, containerPort: 9104}
          env:
            - name: A2A_PUBLIC_URL
              value: "http://response-agent.agent-zone.svc.cluster.local:9104/"
          envFrom:
            - configMapRef: {name: agent-common-config}
          resources:
            requests: {cpu: 250m, memory: 512Mi}
            limits: {cpu: "1", memory: 1Gi}
---
apiVersion: v1
kind: Service
metadata:
  name: response-agent
  namespace: agent-zone
  labels: {app.kubernetes.io/name: response-agent, zone: agent-zone, tier: agent}
spec:
  selector: {app.kubernetes.io/name: response-agent}
  ports:
    - {name: a2a, port: 9104, targetPort: a2a}
