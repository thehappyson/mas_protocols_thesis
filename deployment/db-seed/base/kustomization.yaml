# db-seed base — the namespace-portable loader Job. Don't apply this directly;
# apply an overlay (../minimal or ../reportable) that pins the namespace to where
# postgres-operational lives.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - job.yaml
